#include "stdafx.h"
#include "fuku_mutation_x64_junk.h"

/*
//junk generation
void fuku_junk_1b(fuku_asm_x64& f_asm, fuku_code_holder& code_holder, linestorage::iterator lines_iter) {

    code_holder.get_lines().insert(lines_iter, f_asm.nop(1));
}
void fuku_junk_2b(fuku_asm_x64& f_asm, fuku_code_holder& code_holder, linestorage::iterator lines_iter) {

    switch (FUKU_GET_RAND(0, 0)) {
    case 0: {

        code_holder.get_lines().insert(lines_iter, f_asm.nop(2));
        break;
    }
    }
}
void fuku_junk_3b(fuku_asm_x64& f_asm, fuku_code_holder& code_holder, linestorage::iterator lines_iter) {

    switch (FUKU_GET_RAND(0, 0)) {
    case 0: {
        code_holder.get_lines().insert(lines_iter, f_asm.nop(3));
        break;
    }
    }
}

void fuku_junk_4b(fuku_asm_x64& f_asm, fuku_code_holder& code_holder, linestorage::iterator lines_iter) {

    switch (FUKU_GET_RAND(0, 0)) {
    case 0: {
        code_holder.get_lines().insert(lines_iter, f_asm.nop(4));
        break;
    }
    }
}

void fuku_junk_5b(fuku_asm_x64& f_asm, fuku_code_holder& code_holder, linestorage::iterator lines_iter) {

    switch (FUKU_GET_RAND(0, 0)) {
    case 0: {
        code_holder.get_lines().insert(lines_iter, f_asm.nop(5));
        break;
    }
    }
}

void fuku_junk_6b(fuku_asm_x64& f_asm, fuku_code_holder& code_holder, linestorage::iterator lines_iter) {

    switch (FUKU_GET_RAND(0, 0)) {
    case 0: {
        code_holder.get_lines().insert(lines_iter, f_asm.nop(6));
        break;
    }
    }
}

void fuku_junk_7b(fuku_asm_x64& f_asm, fuku_code_holder& code_holder, linestorage::iterator lines_iter) {

    switch (FUKU_GET_RAND(0, 0)) {
    case 0: {
        code_holder.get_lines().insert(lines_iter, f_asm.nop(7));
        break;
    }
    }
}
*/